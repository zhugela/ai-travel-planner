spring:
  application:
    name: image-search-mcp-server
  profiles:
    active: sse
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 0.0.1
        type: SYNC

server:
  port: 8127

pexels:
  api-key: REPLACE_WITH_YOUR_PEXELS_API_KEY
