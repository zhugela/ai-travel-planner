# 测试专用配置
# 1. 禁用 MCP 客户端（不连接真实 MCP 服务）
# 2. 提供 DashScope 占位符 key（让 Spring 容器能启动）
# 注意：不排除 ToolCallingAutoConfiguration，因为 DashScope 依赖它

spring:
  ai:
    dashscope:
      api-key: test-dummy-key-only-for-spring-context-load
    mcp:
      client:
        enabled: false
